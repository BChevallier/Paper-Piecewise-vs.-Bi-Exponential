# Dependencies for this repository's Python pipeline.
#
# The treadmill pipeline (treadmill/scripts/) needs everything below except
# openpyxl. The bike pipeline (bike/scripts/) additionally needs openpyxl to
# read bike/data/raw/delay_anmedu.xlsx.
pandas>=2.0
numpy>=1.24
scipy>=1.10
pwlf>=2.2
matplotlib>=3.7
openpyxl>=3.1
