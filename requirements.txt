# Dependencies for this repository's Python pipeline.
#
# Treadmill pipeline (code/data_processing, code/vo2_modeling,
# code/data_visualization) needs everything below except openpyxl.
#
# Bike pipeline (bike_code/, code/data_processing/bike/) additionally needs
# openpyxl to read delay_anmedu.xlsx; that pipeline is a work in progress
# and not yet part of the published analysis.
pandas>=2.0
numpy>=1.24
scipy>=1.10
pwlf>=2.2
matplotlib>=3.7
openpyxl>=3.1
